# Core Flask dependencies
flask==2.2.5
werkzeug==2.2.3
gunicorn==20.1.0
flask-cors==4.0.0

# Data processing (lightweight versions)
numpy>=1.21.0,<1.25.0
pandas==1.5.3

# Database
neo4j==5.15.0

# Web requests and APIs
requests==2.28.2

# Environment and configuration
python-dotenv==1.0.0

# Image processing (essential only)
pillow==9.5.0

# Machine Learning (lightweight - only essential packages)
torch>=1.11.0,<2.0.0 --index-url https://download.pytorch.org/whl/cpu
transformers>=4.20.0,<5.0.0
tokenizers>=0.13.0

# Additional utilities for robust deployment
typing-extensions>=4.0.0
certifi>=2022.0.0
charset-normalizer>=2.0.0
idna>=3.0.0
urllib3>=1.26.0

# System monitoring (optional but helpful for deployment)
psutil>=5.9.0

# Text processing
regex>=2022.0.0

# JSON handling
jsonschema>=4.0.0

# Specific transformers dependencies for BLIP
safetensors>=0.3.0
accelerate>=0.20.0